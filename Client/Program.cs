using System;

namespace Client
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            REST CALL:
            POST /api/image  ->  invio jpg a server che risponde con predictions (json)
             */
        }
    }
}
